Apply a Codex-style patch to one or more files.

- Use this when the model outputs patches in `*** Begin Patch` / `*** End Patch` format.
- Supports adding files, deleting files, and updating files with context-based matching.
- Multi-file patches in a single call.
- Fuzzy matching handles minor whitespace differences.
