Quick project health check. Scans for TODOs, FIXMEs, HACKs, and git status.

Sections:
- `todos`: Find TODO/FIXME/HACK/XXX comments in source files.
- `git_status`: Show pending git changes (porcelain format).
- `all` (default): Both sections.

Scope: file or directory path (default: cwd).
